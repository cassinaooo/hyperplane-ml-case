# hyperplane-ml-case

It should be possible to run all numbered notebooks/files in order given `./data/V2_ENG_interview_dataset.csv`.

You can also fetch all the data from a bucket with `$ gsutil cp gs://hyperplane-ml-bucket/data/ ./data` (6.2G). 

If you want access to this bucket please request it.
